{
  "name": "playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "pycodestyle .",
    "hook-add": "prepush install",
    "hook-remove": "prepush remove"
  },
  "prepush": [
    "npm run lint --silent"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "pre-push": "^0.1.1"
  }
}
